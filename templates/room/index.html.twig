

{% extends 'base.html.twig' %}
{% block title %}Room index{% endblock %}

{% block body %}

{% block alerts %}
{% for type, messages in app.session.flashBag.all %}
  {% for message in messages %}
      {%if type == 'error'%} {% set type = 'danger' %} {%endif%}
      {%if type == 'message'%} {% set type = 'info' %} {%endif%}
      {# Bootstrap alert, see http://getbootstrap.com/components/#alerts #}
      <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
          <button type="button" class="close" data-bs-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
              <span class="sr-only">Close</span>
          </button>
          <p>{{ message|raw }}</p>
      </div>
  {% endfor %}
{% endfor %}
{% endblock %} 
   <!-- <h1>Room index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Summary</th>
                <th>Description</th>
                <th>Capacity</th>
                <th>Superficy</th>
                <th>Price</th>
                <th>Address</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for room in rooms %}
            <tr>
                <td>{{ room.id }}</td>
                <td>{{ room.summary }}</td>
                <td>{{ room.description }}</td>
                <td>{{ room.capacity }}</td>
                <td>{{ room.superficy }}</td>
                <td>{{ room.price }}</td>
                <td>{{ room.address }}</td>
                <td>
                    <a href="{{ path('room_show', {'id': room.id}) }}">show</a>
                    <a href="{{ path('room_edit', {'id': room.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('room_new') }}">Create new</a>
-->
    
   
    <head>  <link rel="stylesheet" href="/room.css"></head>
    <body>
        <div class="hero-container">
            {% for room in rooms %}
          <div class="main-container">
           
            <div class="poster-container">
              <a href="{{ path('room_show', {'id': room.id}) }}"><img  src="{{ vich_uploader_asset(room, 'imageFile') }}" class="poster" /></a>
            </div>
            <div class="ticket-container">
              <div class="ticket__content">
                <h4 class="ticket__movie-title">{{ room.summary }}</h4>
                <p class="ticket__movie-slogan">
                  More human than human is our motto.
                </p>
                <p class="ticket__current-price">$28.00</p>
                <p class="ticket__old-price">$44.99</p>
                <a href="{{path('reservation_new', {'id': room.id})}}" class="btn btn_success ticket__buy-btn">Book now</a>
              </div>
            </div>
          </div>
          {% else %}
          <tr>
              <td colspan="8">no records found</td>
          </tr>
      {% endfor %}
        <!--  <div class="main-container">
            <div class="poster-container">
              <a href="#"><img src="https://i.ibb.co/Zd51z5c/justice-league.jpg" class="poster" /></a>
            </div>
            <div class="ticket-container">
              <div class="ticket__content">
                <h4 class="ticket__movie-title">Justice League</h4>
                <p class="ticket__movie-slogan">You can't save the world alone.</p>
                <p class="ticket__current-price">$20.75</p>
                <p class="ticket__old-price">$40.99</p>
                <button class="ticket__buy-btn">Buy now</button>
              </div>
            </div>
          </div>-->
        </div>
        <a href="{{ path('room_new') }}">Create new</a>
      </body>

{% endblock %}
